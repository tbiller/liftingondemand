{
  "name": "lifting-on-demand",
  "version": "0.2.5",
  "description": "A new way to watch powerlifting",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start-dev": "nodemon server.js",
    "heroku-postbuild": "cd client/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.2",
    "deep-equal": "^1.0.1",
    "express": "^4.15.4",
    "mongoose": "^4.11.7",
    "react-cookie": "^2.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}